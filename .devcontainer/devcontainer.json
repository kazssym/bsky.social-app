{
    "image": "mcr.microsoft.com/devcontainers/java:17",
    "features": {
        "ghcr.io/devcontainers-extra/features/typescript:2": {}
    },
    "remoteUser": "root",
    "onCreateCommand": {
        "apt": "sudo apt-get update -y && sudo apt-get install -y android-sdk && apt-get autoclean -y",
        "yarn": "yarn install"
    }
}
